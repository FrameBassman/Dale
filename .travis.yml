language: java
jdk:
  - oraclejdk8
dist: trusty
services:
  - xvfb
before_cache:
  - rm -f  .gradle/caches/modules-2/modules-2.lock
  - rm -fr .gradle/caches/*/plugin-resolution/
cache:
  directories:
    - .gradle/caches/
install:
  - ./gradlew --version
  - ./gradlew assemble --info
script:
  - ./gradlew --project-dir Application build
  - ./gradlew --project-dir E2eTests build

notifications:
  email: false
